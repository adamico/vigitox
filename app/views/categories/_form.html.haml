= simple_form_for categorie, html: {class: 'form-inline'} do |f|
  = f.error_notification if f.error_notification
  = f.text_field :name, placeholder: "libéllé"
  = f.submit "Enregistrer", class: "btn-success", disable_with: "Enregistrer"
