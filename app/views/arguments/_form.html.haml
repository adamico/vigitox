.clear
- semantic_form_for @argument do |f|
  = f.error_messages
  - f.inputs do
    = f.input :name
    = f.input :nature, :as => :select, :collection => Argument::NATURES
  - f.buttons do
    = f.commit_button 'Submit'
