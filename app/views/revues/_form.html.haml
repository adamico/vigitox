.clear
- semantic_form_for @revue, :html => { :multipart => true } do |f|
  = f.error_messages
  - f.inputs do
    = f.input :numero, :label => 'Numéro'
    = f.input :date_sortie, :label => 'Sortie', :discard_day => true, :include_blank => true
    = f.input :pdf_url, :label => 'Lien PDF'
  - f.buttons do
    = f.commit_button "Enregistrer"
