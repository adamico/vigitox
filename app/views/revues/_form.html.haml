-# encoding: utf-8
- semantic_form_for @revue, :html => { :multipart => true } do |f|
  - f.inputs do
    = f.input :numero, :label => 'Numéro'
    = f.input :date_sortie, :label => 'Date de sortie', :discard_day => true, :include_blank => true
    = f.input :pdf
  - f.buttons do
    = f.commit_button "Enregistrer"
