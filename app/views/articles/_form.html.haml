- semantic_form_for [@revue, @article] do |f|
  - f.inputs do
    = f.input :titre, :label => 'Titre'
    = f.input :contenu, :label => 'Contenu'
    = f.input :categories, :as => :check_boxes
  - f.buttons do
    = f.commit_button "Enregistrer"
