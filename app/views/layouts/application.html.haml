!!!
%html
  %head
    %title= yield(:title) || "Vigitox"
    %meta{name: "google-site-verification", content: "H-uhyn8tya-MM12cCc5LgsWVGhT7mrDCAB-Qn9Xe8Js"}
    %meta{:name => "viewport", :content => "width=device-width, initial-scale=1.0"}
    = stylesheet_link_tag    "application", media: "all"
    = javascript_include_tag "application"
    = csrf_meta_tag
    = favicon_link_tag
    = yield(:head)

  %body{class: (kontroller = params[:controller]), id: kontroller}
    %header
      = render 'layouts/navigation'
    %main{role: "main"}
      .container
        %header#page_header
          = yield(:page_header)
          %h1
            = yield(:title) if show_title?
            %small= yield(:subtitle)
          = yield(:subnav)

        #main.clearfix
          = yield

    %footer#footer
      .btn-group
        = link_to "mailto:centre.antipoison@chu-lyon.fr", class: "btn btn-link" do
          %span.glyphicon.glyphicon-envelope
          Nous contacter
        = link_to disclaimer_path, class: "btn btn-link" do
          %span.glyphicon.glyphicon-exclamation-sign
          Mentions légales

      = yield :footer
