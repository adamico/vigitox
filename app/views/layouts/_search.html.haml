= form_tag search_path, method: :get, class: "navbar-search form-search pull-right" do
  .input-append
    = text_field_tag :search, {}, {type: "search", placeholder: "Rechercher par mot clé",
      class: "span2 search-query", value: (params[:search] if params[:search])}
    = button_tag type: "submit", name: nil, class: "btn", id: "search_button" do
      %i.icon-search
